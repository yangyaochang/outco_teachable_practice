const distinctSubsequences = (s, t) => {
    let num = 0

    const traverse = (s_index, t_index) => {
        if (s_index === s.length - 1) {
            return
        }
    }

    traverse(0, 0)
    return num
}

console.log(distinctSubsequences('rebbbit', 'rabbit'))